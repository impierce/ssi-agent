# Schemathesis

This script configures and runs a tool called [Schemathesis](https://schemathesis.readthedocs.io).
It generates a lot of test cases based on the OpenAPI and runs them against a running server.

It requires Python 3 to be installed along with the `uv` Python package manager.

```bash
./check.sh
```
