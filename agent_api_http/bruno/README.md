# Bruno

We try to automate API testing by importing the generated OpenAPI specification into Bruno, provide some environment variables and make it suitable for local testing.

**1. Allow the script to be executed**

```bash
chmod +x run.sh
```

**2. Run the script**

```bash
./run.sh
```

**3. Open the generated collection in the Bruno app**
